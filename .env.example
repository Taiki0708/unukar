# Optional HTTPS form endpoint accepting POST JSON { email }.
NEXT_PUBLIC_SIGNUP_ENDPOINT=
# Set to the final deployed origin for canonical and social metadata.
NEXT_PUBLIC_SITE_URL=

# Server only. Never prefix this with NEXT_PUBLIC_.
OPENAI_API_KEY=
